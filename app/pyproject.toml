[project]
name = "transitar-backend"
version = "0.1.0"
description = "Backend API for TransitAR"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.138.1",
    "sqlalchemy>=2.0.0",
    "uvicorn>=0.49.0",
]